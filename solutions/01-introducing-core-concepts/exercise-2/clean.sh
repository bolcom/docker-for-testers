#!/usr/bin/env bash

srcPath=../../../labs/01-build-your-own
targetDir="../exercise-1/target"

# Remove while target directory,
# to cleanup the compiled version of the application
rm -rf "${targetDir}"
